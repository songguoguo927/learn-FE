const reverseWord = require('../../code/string/557.reverseWords')

test('reverseWord:Let\'s take leetcode contest' , () => {
  expect(reverseWord("Let's take leetcode contest")).toBe("s'teL ekat edocteel tsetnoc");
});